services:
  app:
    image: fwb-task-list-app:1.0.0
    env_file:
      - ../.env

  nginx:
    image: fwb-nginx:1.0.0
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./etc/ssl:/etc/ssl