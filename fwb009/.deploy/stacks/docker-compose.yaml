services:
  app:
    image: gvbusa/fwb-task-list:1.0.0
    env_file:
      - .env

  nginx:
    image: arm64v8/nginx:latest
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx:/etc/nginx
      - ./ssl:/etc/ssl